interface:
  display_name: "Delivery Director"
  short_description: "Direct verified multi-host delivery lanes"
  default_prompt: "Use $delivery-director to direct this multi-lane delivery through verified host tasks and bounded host-local subagents."
