# Residual Review Findings — feat/fusion-manufacturing-intent (issue #18)

Source: ce-code-review run 20260818-224210-f313b630 (correctness + adversarial on Opus, testing and standards/maintainability on Sonnet; cross-model peer prohibited by the user's Claude-only constraint, so the adversarial lens ran as the in-process fallback). Every actionable finding was applied in-branch. The advisory residuals below are the durable record; no tracker tickets filed.

- Advisory — `src/fusion_design/report_diff.py` does not read the new `occurrence_transforms`, so an in-place rotation with an unchanged axis-aligned bounding box diffs as unchanged. Candidate improvement when issue #23's variant evidence lands.
- Advisory — `Manifest.from_data(data, validate=False)` is public and reaches `emit_export_script` without re-validation. No production caller today (tests only); the new coverage guard now fails closed on missing intent, but a caller could still push otherwise-unvalidated intent into the index.
- Advisory — the JSON schema is deliberately weaker than the Python validator: it cannot express path-set equality with `verification.expected_print_parts`, component-tree membership, unique ids/paths, `allowed_alternatives != contact_face`, or `material.source_id` resolution. Fine while the Python validator stays authoritative; the parity test covers enum values and property names only, not verdict equivalence on a whole document.
- Advisory — the schema's `if` at `$defs.printable_part.allOf[0]` omits `required: ["support_policy"]`, so a part missing `support_policy` would be schema-required to carry `support_regions` while the validator forbids it. Latent only because `support_policy` is in the part's required list.
- Advisory — STEP artifacts export `occurrence.component` while each index entry records one `body_name`; an occurrence carrying surface or zero-volume bodies ships more geometry than the recorded name implies. `export_scope: "component"` does disclose this.
- Note — `printable_parts` may be omitted while `verification.expected_print_parts` is populated (intentional, back-compat, pinned by a test). Consumers therefore cannot distinguish "no intent declared" from "intent dropped"; a `declared_intent: false` marker would separate them if that ambiguity ever bites.
- Post-merge obligation (AGENTS.md release coupling): repin the marketplace — `scripts/repin agent-utilities <merge-sha> 0.8.0` from a novotnyllc/marketplace checkout.
